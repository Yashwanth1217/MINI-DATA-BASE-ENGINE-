# MINI-DATA-BASE-ENGINE-
A lightweight C++ relational database engine. Implements custom file storage architectures, B+ tree indexing for fast data retrieval, an efficient query parser, and robust core CRUD operations optimized for low-memory environments.
